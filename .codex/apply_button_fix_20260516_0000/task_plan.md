# Task Plan

## Goal
Fix the browser `Apply` action so it can write an artist into the active ComfyUI V3 Autogrow artist slot.

## Steps
- Inspect ComfyUI custom-node skill guidance.
- Trace the frontend `Apply` path from browser card to node slot write.
- Verify current ComfyUI frontend primitive-node behavior.
- Patch the primitive creation and detection path.
- Run cheap syntax/backend validation.

